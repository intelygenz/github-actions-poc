apiVersion: v1
kind: Service
metadata:
  name: {{ include "actions-poc.fullname" . }}
  labels:
    {{- include "actions-poc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "actions-poc.selectorLabels" . | nindent 4 }}
